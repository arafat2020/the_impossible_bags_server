import { GErrorHandlerFilter } from './g-error-handler.filter';

describe('GErrorHandlerFilter', () => {
  it('should be defined', () => {
    expect(new GErrorHandlerFilter()).toBeDefined();
  });
});
